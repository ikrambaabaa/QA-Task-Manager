stages:
  - install
  - build

install_dependencies:
  stage: install
  image: node:20

  script:
    - npm install

build_app:
  stage: build
  image: node:20

  script:
    - npm install
    - npm run build

  artifacts:
    paths:
      - dist/